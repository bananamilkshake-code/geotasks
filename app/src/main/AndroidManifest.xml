<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.jtalk.android.geotasks" >

    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AddCalendarActivity" >
        </activity>
        <activity
            android:name=".activity.EventsActivity"
            android:label="@string/title_activity_events"
            android:parentActivityName=".activity.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="me.jtalk.android.geotasks.activity.MainActivity" />
        </activity>
        <activity
            android:name=".activity.AddEventActivity"
            android:label="@string/title_activity_add_event" >
        </activity>
    </application>

</manifest>
